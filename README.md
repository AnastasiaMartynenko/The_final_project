# The_final_project
# GeekBrains, C#
1-я четверть
Алгоритм решения:
1. Объявляю первоначальный массив (данные этого массива я задаю сама)
2. Объявляю еще один массив finalArray такой же длины, как и первоначальный
3. Пишу метод void, который на вход принимает первоначальный и конечный массивы
4. Внутри метода объявляю переменную count и присваиваю ей знчение ноль
5. Далее внутри этого метода пишу цикл for (где index = 0; index < длины массива, index = index + 1)
6. Внутри цикла после счетчика пишу условие if (где если длина индекса должна быть <= 3)
7. Если это условие соблюдается, то элемент под данным индексом в первом массиве записывается в count элемента из второго массива.
8. Увеличиваем count на 1 и опять возвращаемся в цикл for (и делаем так по кругу пока не закончатся (пока не переберем) все элементы)
9. 
Это был первый и основной метод